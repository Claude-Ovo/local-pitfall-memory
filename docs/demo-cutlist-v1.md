# LPM-Demo-v1 剪单（主片，41.0 s）

源：`D:\录像\2026-08-27 05-21-19.mp4`（take 2，1920×1080/60，4:31，她 05:21 录）。草稿 `docs/collab/jianying/LPM-Demo-v1/`，已搬入剪映库并注册（`docs/.tmp/generate-demo-v1.py` 生成，`register-draft.py` 注册；改切点改脚本重跑 `--force` + `--replace`）。全部剪映原生元素：视频段（含素材裁切）、文本轨、BGM 轨；没有烧进画面的东西。

| # | 时间线 | 源 in–out | 画面处理 | 下字幕 |
|---:|---|---|---|---|
| 头卡 | 0.0–2.0 | 0.5（静帧，空提示符） | 全画幅 | 「LOCAL PITFALL MEMORY」/「同一个坑，第二次：本地命中，不再从头猜」 |
| 1 | 2.0–10.5 | 0:05.5–0:14.0 | 92% | 第二次遇到同一个坑 |
| 2 | 10.5–14.5 | 0:56.5–1:00.5 | 92% | Qoder 自己跑命令，把完整报错交给踩坑库 |
| 3 | 14.5–17.0 | 1:12.5–1:15.0 | 92% | Skill local-pitfall-memory activated |
| 4 | 17.0–26.0 | 1:33.5–1:42.5 | 素材上裁 11%（去掉 Write 红行），100% | exact · 可引用 —— 本机已验证的修复，本地命中 < 1 秒 |
| 5 | 26.0–28.0 | 2:27.0–2:29.0 | 上裁 70%，2.0×，x=1.0（只看底部三行） | 照修复卡改一行 |
| 6 | 28.0–33.5 | 2:36.5–2:42.0 | 同上 | 一次过 |
| 7 | 33.5–39.5 | 4:13.5–4:19.5 | 上裁 68%，1.25×，x=0.25 | digest：一键汇编本机踩坑表 |
| 尾卡 | 39.5–42.0 | 0.5（静帧） | 全画幅 | 「local-pitfall-memory」/「Qwen3-4B INT4 @ OpenVINO · 历史库与检索不出机 · 魔搭技能中心」 |

BGM：`bgm-pulsar-55s.mp3`（Sensei 同款，YouTube 音频库，零版权风险）音量 0.13，跟到片尾。字体统一指到 `C:/Windows/Fonts/msyhbd.ttc`（微软雅黑粗体，中文安全）——剪映如果不认，在字体面板随便选一个粗体中文字重设即可。

## 为什么这样切

- 去掉了：她在 Qoder 里误敲 `npm pkg get type`（0:28）、按 `?` 弹出的快捷键面板（0:44）、Qoder 请求执行 `node index.js` 的权限框（1:04）、她选 4. No 取消自动修复的过程（1:52）、/quit 与 Interaction Summary（Success Rate 60% 那块）、慢速打字、digest 路径少打一个 s 的红错（3:48）、窗口切换（4:02）。
- 第 4 段刻意选了 Qoder **询问是否执行修复** 的静止画面：exact/可引用的 JSON、Qoder 的中文结论、"Allow this command to run?" 三样同框——"库给方案、人来拍板"这句话不用旁白就说完了。
- 第 5–7 段用裁切放大而不是全画幅，是因为全画幅里挂着 Interaction Summary 的 60% 成功率和 typo 红错；裁掉比解释省事。

## 她要做的

关掉剪映再打开（外部加进去的草稿要重启才看得见），打开 `LPM-Demo-v1`：看字体、看第 4 段裁切上沿有没有露出红行、看第 7 段表格右边被切多少；不满意直接拖。导出 1080p，放 `C:\Users\miku\Videos\魔搭\`。
